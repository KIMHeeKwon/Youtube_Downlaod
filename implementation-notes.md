# implementation-notes — Phase 3 구현 (2026-08-01)

작업 단위 임시 파일. 승인된 계획([[PHASE3]])에서 이탈한 지점과 그 근거를 남긴다.

## Deviations

### 1. 교체 스크립트를 `.bat`/`.sh` 대신 Python 스크립트로 생성

- **계획**: [[PHASE3#5.2 절차|§5.2]] "교체 스크립트 생성 (Windows `.bat` / macOS `.sh`)"
- **실제**: 단일 Python 스크립트를 임시 폴더에 쓰고 `sys.executable`로 실행
- **근거**: 셸 두 벌을 유지하면 경로 인용·이스케이프 버그가 OS별로 따로 생긴다. 교체 대상에
  `.venv/`가 포함되지 않으므로([[PHASE3#5.1 교체 대상 범위|§5.1]]) 앱이 종료된 뒤에도
  `sys.executable`이 계속 유효하다 — Python 스크립트를 쓸 수 있는 전제가 이미 성립해 있었다.
  실패 시 백업 복원 로직도 한 곳에만 두면 된다.

### 2. 부모 프로세스 종료 대기를 PID 폴링 대신 고정 대기(2초)로 단순화

- **계획**: [[PHASE3#5.2 절차|§5.2]] "앱 종료 → 교체 스크립트가 파일 복사"
- **실제**: `time.sleep(2)` 후 복사 진행
- **근거**: `os.kill(pid, 0)`으로 생존 확인하는 방식이 Windows에서 동작하지 않아 플랫폼별
  분기가 필요한데, 교체 대상이 `.py` 소스뿐이라 Windows에서도 파일 잠금이 걸리지 않는다
  (잠기는 것은 실행 중인 `.exe`와 `.venv`이며 둘 다 교체 대상이 아니다). 복잡한 생존 확인이
  막아주는 실패 모드가 실재하지 않아 보수적으로 단순화했다.

### 3. README 내부 문서 참조는 마크다운 링크 유지

- **계획/규약**: `doc-style.md`는 모든 `.md` 산출물의 내부 참조를 `[[위키링크]]`로 쓰도록 한다
- **실제**: `README.md`에 추가한 문서 표 항목은 기존 마크다운 링크 형식을 따랐다
- **근거**: 해당 표의 기존 4개 행이 전부 마크다운 링크이며, README는 GitHub 웹에서 렌더링되는
  공개 진입점이다. 새 행만 위키링크로 쓰면 같은 표 안에서 절반은 링크·절반은 생텍스트가 된다.
  신규 문서([[DECISIONS]], [[PHASE3]]) 본문은 규약대로 위키링크를 사용했다.

## 검증 기록

| 항목 | 결과 |
|------|------|
| 단위 테스트 | 31/31 통과 (updater 12건 신규 — 버전 비교 자릿수 함정, 릴리스 파싱, 교체 범위) |
| `gui.py --selftest` | 통과 (투어 생성/파괴 포함) |
| 투어 스포트라이트 기하 | 4단계 전부 통과 — 스포트라이트 미차폐, 창 모서리 딤 적용, 말풍선 비중첩·화면 내 |
| 화면 캡처 육안 확인 | **미수행** — 터미널에 macOS 화면 기록 권한이 없어 `screencapture` 실패 |

## 미해결

- 업데이트 배너 → 교체 → 재시작 **엔드투엔드 실증**은 릴리스가 하나도 발행되지 않아 불가.
  v0.2.0 발행 후 구버전 설치본에서 확인 필요 ([[PHASE3#8. 검증 기준|§8]]).
